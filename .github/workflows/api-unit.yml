name: Legacy API atomic-unit dispatch (superseded)

on: workflow_dispatch

permissions:
  contents: read

jobs:
  legacy-authority-superseded:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse superseded legacy dispatch
        run: |
          echo "legacy-authority-superseded: #57 revoked the old 124-API Phase C authority."
          echo "Use the next-generation work tracked by #56."
          exit 1
