name: Issue 261 compiler VFS checkpoint

on:
  push:
    branches:
      - agent/issue-261-source-closure-vfs-implementation

permissions:
  contents: read

concurrency:
  group: issue-261-compiler-vfs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compiler-vfs:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: ./.github/actions/setup-ci
        with:
          profile: developer
          documentation: "false"
      - name: Compile and run source-closure and compiler-VFS checkpoints
        run: |
          set -euo pipefail
          clang++-22 -std=c++23 -Wall -Wextra -Wpedantic -Werror \
            -Isrc \
            src/llvm/clang22/source_closure.cpp \
            tests/adapter/clang22/source_closure_test.cpp \
            -o "${RUNNER_TEMP}/cxxlens-source-closure-test"
          "${RUNNER_TEMP}/cxxlens-source-closure-test"
          clang++-22 -std=c++23 -Wall -Wextra -Wpedantic -Werror \
            -Isrc \
            src/llvm/clang22/source_closure.cpp \
            src/llvm/clang22/compiler_vfs.cpp \
            tests/adapter/clang22/compiler_vfs_test.cpp \
            -o "${RUNNER_TEMP}/cxxlens-compiler-vfs-test"
          "${RUNNER_TEMP}/cxxlens-compiler-vfs-test"
      - name: Verify branch diff hygiene
        run: git diff --check HEAD^ HEAD
